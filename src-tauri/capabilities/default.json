{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Editor and Output windows",
  "windows": ["main", "output"],
  "permissions": ["core:default"]
}
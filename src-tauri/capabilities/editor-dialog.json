{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "editor-dialog",
  "description": "Native file dialogs for the Editor window only (media import, settings export/import). Output and Stage windows never open dialogs, so they are deliberately not granted this.",
  "windows": ["main"],
  "permissions": ["dialog:default"]
}